

function uno(a : Int) -> Int{
	var i : Int;
	i = a * 2;
	if (i < a + 4){
		uno(a + 1);
	}
	print(i);
}

function dos(b : Int) -> Int{
	b = b * b + b;
	return (b*2);
	return (b*2);
}

main(){
	var i : Int; 
	var j : [10]Int;
	i = 2;
	i = i * 2 - 1;
	uno(j);
	input(j[10]);
	//print(i + dos(i + j));
}