function factorial( n : Int) -> Int{

	var resultado : Int;
	resultado = 1;

	while(n > 1){
		resultado = resultado * n;
		n = n - 1;
	}
	return resultado;
}


main(){
	var x : Int;
	print("Numero para calcular el factorial:");
	input(x);
	print(factorial(x));
}